SDRVTEST is a tool for testing whether a device or a SCSI Driver
implementation handles certain SCSI commands in compliance with the current
SCSI specification (http://www.t10.org).
The device does not necessarily have to be a SCSI device, but can be any
kind of device supported by a SCSI Driver. HDDRIVER provides SCSI Drivers not
only for SCSI devices but also for ACSI, IDE, ATAPI and SATA devices.
Any error reported by SDRVTEST ("ERROR" is displayed) means that most likely
something is either wrong with how the device handles SCSI commands, or with
the SCSI Driver managing the respective bus. If an optional SCSI command is
not supported this is not considered an error.
Applications may not work correctly with devices or SCSI Drivers for which
the executed tests fail.
SDRVTEST saves the test results in a file SDRVTEST.LOG in the current
directory and also displays the results on the screen.
NOTE: Currently only LUN 0 is tested.

MCHGTEST, ERRTEST and SERRTEST test other SCSI Driver features, related to
error handling and media changes. Please refer to the sources for details.
Excepot for writing the SDRVTEST log file the tests do not write any data.

--

SDRVTEST ist ein Tool, mit dem sich testen lÑsst, ob ein GerÑt oder eine
SCSI-Treiber-Implementierung gewisse SCSI-Kommandos in öbereinstimmung
mit der aktuellen SCSI-Spezifikation (http://www.t10.org) ausfÅhrt.
Das GerÑt muss nicht unbedingt ein SCSI-GerÑt sein, sondern es kann sich um
ein beliebiges GerÑt handeln, das von einem SCSI-Treiber unterstÅtzt wird.
HDDRIVER stellt SCSI-Treiber nicht nur fÅr SCSI- sondern auch fÅr ACSI-,
IDE-, ATAPI- und SATA-GerÑte bereit.
Jeder von SDRVTEST gemeldete Fehler ("ERROR" wird angezeigt) bedeutet mit
hoher Wahrscheinlichkeit, dass entweder das GerÑt nicht korrekt mit
SCSI-Kommandos umgeht, oder dass etwas mit der SCSI-Treiber-Implementierung
fÅr diesen Bus nicht stimmt. Falls ein optionales SCSI-Kommando nicht
unterstÅtzt wird, wird dies nicht als Fehler angesehen.
Anwendungen funktionieren mîglicherweise nicht korrekt mit GerÑten oder
SCSI-Treibern, bei denen die durchgefÅhrten Tests fehlschlagen.
SDRVTEST sichert die Testergebnisse in einer Datei SDRVTEST.LOG im aktuellen
Verzeichnis und zeigt die Ergebnisse auch auf dem Bildschirm an.
HINWEIS: Zur Zeit wird nur LUN 0 getestet.

MCHGTEST, ERRTEST and SERRTEST testen weitere SCSI-Treiber-Features, die sich
auf Fehlerbehandlung und Medienwechsel beziehen. Bitte entnehmen Sie Details
den Quelltexten.
Bis auf das Schreiben des SDRVTEST-Logfiles schreiben die Tests keine Daten.


  Uwe.Seimet@seimet.de, 2022-03-13
