SDRVTEST is a small tool for testing whether a device or a SCSI Driver
implementation handles certain SCSI commands in compliance with the SCSI
specification (http://www.t10.org).
The device does not necessarily have to be a SCSI device, but can be any
kind of device supported by a SCSI driver. HDDRIVER, for instance, provides
a SCSI xdriver not only for SCSI devices but also for ACSI, IDE, ATAPI and
SATA devices.
Any error reported by SDRVTEST means that most likely something is either
wrong with the way the device handles SCSI commands, or with the SCSI Driver
managing this bus. If an optional SCSI command is not supported by a device
or SCSI Driver this is not considered an error.
Applications making use of the SCSI Driver interface may not work correctly
with SCSI Drivers where some of the tests executed by SDRVTEST fail.
SDRVTEST saves the test results in a file SDRVTEST.LOG in the current
directory and also displays the results on the screen.
A comma-separated list of bus IDs to be checked can be passed as an argument.
Without an argument all available buses are checked.

MCHGTEST, ERRTEST and SERRTEST test other SCSI Driver features, related to
error handling and media changes. Please refer to the sources for details.

--

SDRVTEST ist ein kleines Hilfsprogramm, mit dem sich testen lÑsst, ob ein
GerÑt oder eine SCSI-Treiber-Implementierung gewisse SCSI-Kommandos in
öbereinstimmung mit der SCSI-Spezifikation (http://www.t10.org) ausfÅhrt.
Das GerÑt muss nicht unbedingt ein SCSI-GerÑt sein, sondern es kann sich um
ein beliebiges GerÑt handeln, das von einem SCSI-Treiber unterstÅtzt wird.
HDDRIVER beispielsweise stellt SCSI-Treiber nicht nur fÅr SCSI- sondern auch
fÅr ACSI-, IDE-, ATAPI- und SATA-GerÑte bereit.
Jeder von SDRVTEST gemeldete Fehler bedeutet mit hoher Wahrscheinlichkeit,
dass entweder das GerÑt nicht korrekt mit SCSI-Kommandos umgeht, oder dass
etwas mit der SCSI-Treiber-Implementierung fÅr diesen Bus nicht stimmt.	Falls
ein optionales SCSI-Kommando von einem GerÑt oder SCSI-Treiber nicht
unterstÅtzt wird, wird dies nicht als Fehler angesehen.
Anwendungen, die das SCSI-Treiber-Interface benutzen, arbeiten mîglicherweise
nicht korrekt mit SCSI-Treibern zusammen, bei denen die von SDRVTEST
durchgefÅhrten Tests fehlschlagen.
SDRVTEST sichert die Testergebnisse in einer Datei SDRVTEST.LOG im aktuellen
Verzeichnis und zeigt die Ergebnisse auch auf dem Bildschirm an.
Es kann eine komma-separierte Liste von zu prÅfenden Bus-IDs als Argument
Åbergeben werden. Ohne Argument werden alle verfÅgbaren Busse getestet.


MCHGTEST, ERRTEST and SERRTEST testen weitere SCSI-Treiber-Features, die sich
auf Fehlerbehandlung und Medienwechsel beziehen. Bitte entnehmen Sie Details
den Quelltexten.


  Uwe.Seimet@seimet.de, 2021-06-12
